[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "omni_util"
version = "0.1.1"
dependencies = [
    "psycopg2-binary",
    "python-dotenv"
]

[tool.setuptools]
py-modules = ["postgres_util"] 